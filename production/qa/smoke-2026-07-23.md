# Smoke Check — Epic SaveManager (Story 007) · 2026-07-23

> **Build**: rama main (autoload SaveManager registrado 5º en project.godot)
> **Godot**: 4.6.stable · headless
> **Veredicto**: ✅ **PASS**

## Comandos ejecutados y resultados

| Comprobación | Comando | Resultado |
|---|---|---|
| Suite completa (unit + integration) | `GdUnitCmdTool.gd -a res://tests/unit -a res://tests/integration --ignoreHeadlessMode` | **135/135 · 0 fallos · exit 0** |
| Arranque real + ciclo guardar/cargar con el singleton | `--headless --script res://tests/smoke_save_manager.gd` | `SMOKE_GUARDAR: true` · `SMOKE_CARGAR: true` · `SMOKE_OK` · exit 0 |
| Arranque del juego (Main.tscn + 5 autoloads) | `--headless --quit-after 200` | Sin SCRIPT ERROR |

## Alcance del smoke

- Los **5 autoloads** arrancan en el orden del control-manifest: EventBus → RNGService → Datos → Tiempo → SaveManager.
- `SaveManager.guardar_partida("user://smoke_test.save")` recolecta del grupo **Persist real** (RNGService + Tiempo) y escribe con el patrón temporal + renombrado.
- `cargar_partida` lee, valida versión y **distribuye** a los sistemas reales; Tiempo queda en Pausa (por su propio `load_state`).
- El archivo del smoke se **borra al terminar** (sin residuos en `user://`).

## Notas

- El único `ERROR` visible en la salida de la suite ("Parse JSON failed... got 'esto'") es un caso de test
  **intencionado** (save corrupto → fallo controlado sin crash, AC de la Story 004).
- Script del smoke: `tests/smoke_save_manager.gd` (standalone `SceneTree`; complementa a la suite, que usa
  instancias aisladas por diseño).
